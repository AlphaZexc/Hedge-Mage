using UnityEngine;
using UnityEngine.Rendering.Universal; // Keep this for the Light2D

// --- SIMPLIFIED SCRIPT ---
// This script's only job is to exist. All interaction logic is now on the player.
public class Fountain : MonoBehaviour
{
    // We are keeping this script on the object so that other scripts
    // could potentially interact with it in the future if needed,
    // but it has no active logic for player interaction anymore.
}